{
  "name": "facachat-worker",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "FACACHAT — Cloudflare Worker that translates natural-language dashboard questions into a structured query plan via the Anthropic API.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^3.78.0"
  }
}
